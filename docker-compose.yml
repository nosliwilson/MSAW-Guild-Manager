services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/guild.db
      - JWT_SECRET=change-this-to-something-secure
    volumes:
      - ./guild.db:/app/guild.db
    restart: always

volumes:
  db_data:
